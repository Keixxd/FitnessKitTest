<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FitnessKitTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnSecondary</item>
        <!-- Customize your theme here. -->
        <item name="toolbarStyle">@style/Theme.FitnessKitTest.MaterialToolBar</item>
    </style>

    <style name="Theme.FitnessKitTest.MaterialToolBar" parent="Widget.AppCompat.Toolbar">
        <item name="background">@color/white</item>
        <item name="minHeight">?attr/actionBarSize</item>
        <item name="contentInsetStart">0dp</item>
    </style>
</resources>